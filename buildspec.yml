version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - java -version
      - echo Build started on `date`
      - mvn test
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - '**/*'
